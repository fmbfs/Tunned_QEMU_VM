#!/bin/bash

#####################################################################################################################################
##### DEFAULTS #####
#####################################################################################################################################
file="config.json"

while read -r line; do
    #echo -e "$line\n"
    #echo ${line} | awk '{print $1}'
    if [[ ${line} != 'Name' ]]; then
        teste= $(echo ${line} | awk '{print $2}')
        echo ${teste}
    fi
done <$file 